package com.company;

import java.util.Random;
import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        // Задан массив А, содержащий 12 целых случайных чисел от -5 до 5. Найти сумму элементов массива
       /* Scanner scanner = new Scanner(System.in);
        int [] array = new int[12];
        for(int i=0; i< array.length; i++){
            array[i] = (int) Math.round((Math.random()*10)-5);
            System.out.println(array[i]);
            int sum= array[i] + array[i];
        }*/

        // Задан массив который содержит 13 случайных целых чисел от -12 до 12.  Найти произведение отрицательных элементов массива
         /*Scanner in = new Scanner(System.in);
        int n, array[], negative = 1;
        System.out.print("Введите размер массива: ");
        n = in.nextInt();
        int i =  in.nextInt();
        array = new int [13];
        for (int i=0 ; i < array.length ; i++){
            array[i] = (int) ( Math.random() * 12+12);
            for (int i: array)
                System.out.print( i + "\n" );
            for (int i: array)
                if ( i < 0)
                    negative *= i;
        }
        System.out.println("Произведение отрицательных эл-тов массива = " + negative);*/
                }
           }
